from setuptools import setup

setup(
    name='shopping',
    version='1.0',
    packages=['shopping', 'util'],
    license='',
    url='https://pyspark.dev',
    author='P',
    description='',
    setup_requires=["pytest-runner"],
    tests_require=["pytest"]
)